{
  "uid": "reeve-overview",
  "title": "Reeve",
  "tags": [
    "reeve"
  ],
  "timezone": "browser",
  "schemaVersion": 39,
  "version": 1,
  "refresh": "1m",
  "time": {
    "from": "now-7d",
    "to": "now"
  },
  "panels": [
    {
      "type": "text",
      "title": "What this shows, and what it does not",
      "gridPos": {
        "h": 5,
        "w": 24,
        "x": 0,
        "y": 0
      },
      "options": {
        "mode": "markdown",
        "content": "Usage and cost as the collector computed it from tokens and your price table. There is no identity here: no emails, no sessions, no repository names. Those live in the event store, which has different access control and different retention, and `reeve report` reads them.\n\n**Computed cost and vendor-reported cost are separate series on purpose. Do not add them together.** They are two measurements of the same thing, kept apart so that a discrepancy is visible rather than averaged away.\n\nRequests whose model was not in your price table are counted separately as unpriced. Treating them as costing nothing would understate the total while the dashboard looked complete.\n\n**The cost figures here are equivalent cost: what the usage would cost at your rates.** For an organisation paying for seats in advance that is not money leaving, which is why the allowance row below exists. `reeve_billing_undeclared_events_total` counts requests belonging to an agent whose billing arrangement you have not declared; no statement about money can be made about those at all.\n\n**A flat zero on \"Allowance used\" is not good news.** It means nothing is being measured against an allowance you declared \u2014 the agent exports no usage, or the export is not reaching the collector. On a graph that is indistinguishable from staying comfortably inside the limit. Alert on it."
      }
    },
    {
      "type": "stat",
      "title": "Equivalent cost, not money (7d)",
      "gridPos": {
        "h": 5,
        "w": 6,
        "x": 0,
        "y": 5
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2
        }
      },
      "targets": [
        {
          "expr": "sum(increase(reeve_equivalent_cost_usd_total[7d]))",
          "legendFormat": "computed",
          "refId": "A"
        }
      ],
      "description": "What the usage would cost at your rates. Under a subscription the seats were bought in advance, so consumption inside the included allowance costs nothing further and this is not a bill. The allowance panels below are the ones to act on."
    },
    {
      "type": "stat",
      "title": "Cost as the vendors reported it (7d)",
      "gridPos": {
        "h": 5,
        "w": 6,
        "x": 6,
        "y": 5
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD",
          "decimals": 2
        }
      },
      "targets": [
        {
          "expr": "sum(increase(reeve_vendor_reported_cost_usd_total[7d]))",
          "legendFormat": "vendor",
          "refId": "A"
        }
      ]
    },
    {
      "type": "stat",
      "title": "Requests with no price for their model",
      "description": "These carry tokens and cost something, and none of it is in the figures to the left.",
      "gridPos": {
        "h": 5,
        "w": 6,
        "x": 12,
        "y": 5
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              },
              {
                "color": "orange",
                "value": 1
              }
            ]
          }
        }
      },
      "targets": [
        {
          "expr": "sum(increase(reeve_events_unpriced_total[7d]))",
          "legendFormat": "unpriced",
          "refId": "A"
        }
      ]
    },
    {
      "type": "stat",
      "title": "Batches the collector could not read",
      "description": "An agent is exporting something this collector does not understand, so that agent's activity is missing from everything else on this page.",
      "gridPos": {
        "h": 5,
        "w": 6,
        "x": 18,
        "y": 5
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "thresholds": {
            "mode": "absolute",
            "steps": [
              {
                "color": "green",
                "value": null
              },
              {
                "color": "red",
                "value": 1
              }
            ]
          }
        }
      },
      "targets": [
        {
          "expr": "sum(increase(reeve_batches_rejected_total[7d]))",
          "legendFormat": "rejected",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Equivalent cost by agent",
      "gridPos": {
        "h": 9,
        "w": 12,
        "x": 0,
        "y": 10
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "currencyUSD"
        }
      },
      "targets": [
        {
          "expr": "sum by (agent) (increase(reeve_equivalent_cost_usd_total[1h]))",
          "legendFormat": "{{agent}}",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Tokens by agent and kind",
      "gridPos": {
        "h": 9,
        "w": 12,
        "x": 12,
        "y": 10
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "targets": [
        {
          "expr": "sum by (agent, kind) (increase(reeve_tokens_total[1h]))",
          "legendFormat": "{{agent}} {{kind}}",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Events written by agent",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 0,
        "y": 19
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "targets": [
        {
          "expr": "sum by (agent) (increase(reeve_events_written_total[1h]))",
          "legendFormat": "{{agent}}",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Is anything still arriving?",
      "description": "Seconds since the event store was last written. A line that climbs forever means the agents stopped exporting, and every other panel here will quietly flatten rather than saying so.",
      "gridPos": {
        "h": 8,
        "w": 12,
        "x": 12,
        "y": 19
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "s"
        }
      },
      "targets": [
        {
          "expr": "time() - reeve_store_modified_timestamp_seconds",
          "legendFormat": "since last write",
          "refId": "A"
        }
      ]
    },
    {
      "type": "text",
      "title": "Included allowance",
      "description": "",
      "gridPos": {
        "h": 3,
        "w": 24,
        "x": 0,
        "y": 27
      },
      "options": {
        "mode": "markdown",
        "content": "What your plan includes, and how much of it has gone. These are the figures a seat-based organisation acts on: the outlay was fixed when the seats were bought, and what varies is whether the included consumption lasts the period.\n\nAbsent rather than zero means the event store could not be read \u2014 see `reeve_allowance_read_errors_total`. Held-over figures would draw a healthy line through an outage, so they are dropped instead."
      }
    },
    {
      "type": "timeseries",
      "title": "Allowance used, as a share of what is included",
      "description": "Above 100% the allowance for that window is gone. What happens next depends on your overage: credits is a bill, throttled is lost time, blocked is an outage.",
      "gridPos": {
        "h": 7,
        "w": 12,
        "x": 0,
        "y": 30
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "percent"
        }
      },
      "targets": [
        {
          "expr": "100 * reeve_allowance_used / reeve_allowance_included",
          "legendFormat": "{{agent}} {{limit}}",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "People past a single seat",
      "description": "The number a share of the total hides. An organisation can sit at forty per cent of its allowance while somebody is at three hundred per cent of theirs. Who they are is deliberately not in the metrics; run reeve report.",
      "gridPos": {
        "h": 7,
        "w": 12,
        "x": 12,
        "y": 30
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short"
        }
      },
      "targets": [
        {
          "expr": "reeve_allowance_seats_over",
          "legendFormat": "{{agent}} {{limit}}",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Pace: will it last the period?",
      "description": "Consumption divided by the rate that would just use the allowance up. Above 1 means it runs out before the period resets. This is the one that fires while there is still time to do something.",
      "gridPos": {
        "h": 7,
        "w": 12,
        "x": 0,
        "y": 37
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short"
        }
      },
      "targets": [
        {
          "expr": "reeve_allowance_pace",
          "legendFormat": "{{agent}} {{limit}}",
          "refId": "A"
        }
      ]
    },
    {
      "type": "timeseries",
      "title": "Consumption nobody could be attributed",
      "description": "In the total, but in nobody's per-seat figure. While this is large, \"people past a single seat\" is a lower bound.",
      "gridPos": {
        "h": 7,
        "w": 12,
        "x": 12,
        "y": 37
      },
      "datasource": {
        "type": "prometheus",
        "uid": "${DS_PROMETHEUS}"
      },
      "fieldConfig": {
        "defaults": {
          "unit": "short"
        }
      },
      "targets": [
        {
          "expr": "reeve_allowance_unattributed",
          "legendFormat": "{{agent}} {{limit}}",
          "refId": "A"
        }
      ]
    }
  ],
  "templating": {
    "list": [
      {
        "name": "DS_PROMETHEUS",
        "type": "datasource",
        "query": "prometheus",
        "current": {
          "text": "Prometheus",
          "value": "Prometheus"
        },
        "hide": 0
      }
    ]
  }
}
