apiVersion: v2
name: reeve-collector
description: Receives AI coding agent telemetry and normalises it into one audit and cost record.
type: application

# The chart is versioned independently of Reeve itself, because a change to a
# Kubernetes manifest is not a change to the collector's behaviour.
version: 0.1.0
appVersion: "0.1.0"

home: https://github.com/feysal07/reeve
sources:
  - https://github.com/feysal07/reeve
keywords:
  - opentelemetry
  - otlp
  - ai-agents
  - governance
  - audit
maintainers:
  - name: Feysal Rujbally
    url: https://github.com/feysal07
